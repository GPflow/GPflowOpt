sudo: required
dist: trusty
language: python
python:
  - 3.6
cache: pip
install:
  - pip install -U pip wheel codecov nox-automation
script:
  - nox -s unit implementations cover
after_success:
  - codecov